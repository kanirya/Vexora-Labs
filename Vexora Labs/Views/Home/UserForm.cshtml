@model ServiceInquiryViewModel

@{
    ViewData["Title"] = "Get in Touch";
}

<!-- Contact Hero Section -->
<section class="contact-hero-section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="text-center mb-5">
                    <h6 class="text-gradient text-uppercase fw-bold">Contact Us</h6>
                    <h1 class="contact-title">Let's Build Something Amazing Together</h1>
                    <p class="contact-subtitle">Tell us about your project and we'll get back to you within 24 hours.</p>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Contact Form Section -->
<section class="contact-form-section section-dark-alt">
    <div class="container">
        <div class="row">
            <div class="col-lg-5 mb-5 mb-lg-0">
                <div class="contact-info-wrapper">
                    <div class="contact-info-header">
                        <h2>How Can We Help?</h2>
                        <div class="divider"></div>
                        <p>Ready to transform your ideas into reality? Our team of experts is here to help you with any tech service you need.</p>
                    </div>

                    <div class="contact-info-content">
                        <div class="contact-info-item">
                            <div class="contact-info-icon">
                                <i class="fas fa-map-marker-alt"></i>
                            </div>
                            <div class="contact-info-text">
                                <h5>Our Location</h5>
                                <p>123 Innovation Street, Tech City, 10001</p>
                            </div>
                        </div>

                        <div class="contact-info-item">
                            <div class="contact-info-icon">
                                <i class="fas fa-phone-alt"></i>
                            </div>
                            <div class="contact-info-text">
                                <h5>Call Us</h5>
                                <p>+1 (123) 456-7890</p>
                            </div>
                        </div>

                        <div class="contact-info-item">
                            <div class="contact-info-icon">
                                <i class="fas fa-envelope"></i>
                            </div>
                            <div class="contact-info-text">
                                <h5>Email Us</h5>
                                <p>info@vexoralabs.com</p>
                            </div>
                        </div>

                        <div class="contact-info-item">
                            <div class="contact-info-icon">
                                <i class="fas fa-clock"></i>
                            </div>
                            <div class="contact-info-text">
                                <h5>Working Hours</h5>
                                <p>Monday - Friday: 9:00 AM - 6:00 PM</p>
                            </div>
                        </div>
                    </div>

                    <div class="contact-social">
                        <h5>Connect With Us</h5>
                        <div class="social-links">
                            <a href="#" class="social-link"><i class="fab fa-twitter"></i></a>
                            <a href="#" class="social-link"><i class="fab fa-facebook-f"></i></a>
                            <a href="#" class="social-link"><i class="fab fa-instagram"></i></a>
                            <a href="#" class="social-link"><i class="fab fa-linkedin-in"></i></a>
                            <a href="#" class="social-link"><i class="fab fa-github"></i></a>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-lg-7">
                <div class="service-inquiry-form">
                    <div class="form-header">
                        <h3>Service Inquiry</h3>
                        <p>Please fill out the form below to help us understand your project better.</p>
                    </div>

<form asp-action="Index" method="post" class="needs-validation" novalidate>
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="row g-4">
        <div class="col-md-6">
            <div class="form-floating">
                <input asp-for="FullName" class="form-control" placeholder="Full Name" required />
                <label asp-for="FullName"></label>
                <span asp-validation-for="FullName" class="invalid-feedback"></span>
            </div>
        </div>
        <div class="col-md-6">
            <div class="form-floating">
                <input asp-for="Email" class="form-control" placeholder="Email Address" required />
                <label asp-for="Email"></label>
                <span asp-validation-for="Email" class="invalid-feedback"></span>
            </div>
        </div>
        <div class="col-md-6">
            <div class="form-floating">
                <input asp-for="Phone" class="form-control" placeholder="Phone Number (Optional)" />
                <label asp-for="Phone"></label>
                <span asp-validation-for="Phone" class="invalid-feedback"></span>
            </div>
        </div>
        <div class="col-md-6">
            <div class="form-floating">
                <input asp-for="Company" class="form-control" placeholder="Company Name (Optional)" />
                <label asp-for="Company"></label>
                <span asp-validation-for="Company" class="invalid-feedback"></span>
            </div>
        </div>

        <div class="col-md-6">
            <div class="form-floating">
                <select asp-for="ServiceType" class="form-select" required>
                    <option value="">Select a service</option>
                    <option value="web-development">Web Development</option>
                    <option value="mobile-app">Mobile Application</option>
                    <option value="cloud-solutions">Cloud Solutions</option>
                    <option value="data-analytics">Data Analytics</option>
                    <option value="cybersecurity">Cybersecurity</option>
                    <option value="ai-ml">AI & Machine Learning</option>
                    <option value="other">Other</option>
                </select>
                <label asp-for="ServiceType"></label>
                <span asp-validation-for="ServiceType" class="invalid-feedback"></span>
            </div>
        </div>
        <div class="col-md-6">
            <div class="form-floating">
                <select asp-for="ProjectBudget" class="form-select">
                    <option value="">Select budget range</option>
                    <option value="less-5k">Less than $5,000</option>
                    <option value="5k-10k">$5,000 - $10,000</option>
                    <option value="10k-25k">$10,000 - $25,000</option>
                    <option value="25k-50k">$25,000 - $50,000</option>
                    <option value="50k-plus">$50,000+</option>
                    <option value="not-sure">Not sure yet</option>
                </select>
                <label asp-for="ProjectBudget"></label>
            </div>
        </div>

        <div class="col-md-6">
            <div class="form-floating">
                <select asp-for="ProjectTimeline" class="form-select">
                    <option value="">Select timeline</option>
                    <option value="asap">As soon as possible</option>
                    <option value="1-month">Within 1 month</option>
                    <option value="1-3-months">1-3 months</option>
                    <option value="3-6-months">3-6 months</option>
                    <option value="6-plus-months">6+ months</option>
                    <option value="not-sure">Not sure yet</option>
                </select>
                <label asp-for="ProjectTimeline"></label>
            </div>
        </div>
        <div class="col-md-6">
            <div class="form-floating">
                <select asp-for="HearAboutUs" class="form-select">
                    <option value="">How did you get us?</option>
                    <option value="search-engine">Search Engine</option>
                    <option value="social-media">Social Media</option>
                    <option value="referral">Referral</option>
                    <option value="advertisement">Advertisement</option>
                    <option value="other">Other</option>
                </select>
                <label asp-for="HearAboutUs"></label>
            </div>
        </div>

        <div class="col-12">
            <div class="form-floating">
                <textarea asp-for="ProjectDescription" class="form-control" style="height:150px" required></textarea>
                <label asp-for="ProjectDescription"></label>
                <span asp-validation-for="ProjectDescription" class="invalid-feedback"></span>
            </div>
        </div>

        <div class="col-12">
            <div class="form-check">
                <input asp-for="PrivacyPolicy" class="form-check-input" />
                <label asp-for="PrivacyPolicy" class="form-check-label"></label>
                <span asp-validation-for="PrivacyPolicy" class="invalid-feedback"></span>
            </div>
        </div>
        <div class="col-12">
            <div class="form-check">
                <input asp-for="Newsletter" class="form-check-input" />
                <label asp-for="Newsletter" class="form-check-label"></label>
            </div>
        </div>

        <div class="col-12 mt-4">
            <button type="submit" class="btn btn-gradient btn-lg w-100">
                <span>Submit Inquiry</span>
            </button>
        </div>
    </div>
</form>



                    <!-- Success Message (Hidden by default) -->
                    <div class="form-success-message" id="formSuccessMessage" style="display: none;">
                        <div class="success-icon">
                            <i class="fas fa-check-circle"></i>
                        </div>
                        <h3>Thank You!</h3>
                        <p>Your inquiry has been submitted successfully. Our team will get back to you within 24 hours.</p>
                        <button type="button" class="btn btn-gradient mt-4" id="resetFormButton">Submit Another Inquiry</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- FAQ Section -->
<section class="faq-section section-dark">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8 text-center mb-5">
                <h6 class="text-gradient text-uppercase fw-bold">FAQ</h6>
                <h2 class="section-title">Frequently Asked Questions</h2>
                <p class="section-subtitle">Find answers to common questions about our services and process.</p>
            </div>
        </div>

        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="accordion" id="faqAccordion">
                    <!-- FAQ Item 1 -->
                    <div class="accordion-item">
                        <h2 class="accordion-header" id="headingOne">
                            <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                                What is the typical process for starting a new project?
                            </button>
                        </h2>
                        <div id="collapseOne" class="accordion-collapse collapse show" aria-labelledby="headingOne" data-bs-parent="#faqAccordion">
                            <div class="accordion-body">
                                Our process typically begins with an initial consultation to understand your requirements. We then provide a detailed proposal including timeline and cost estimates. Once approved, we move to the design phase, followed by development, testing, and deployment. Throughout the project, we maintain regular communication and provide updates on progress.
                            </div>
                        </div>
                    </div>

                    <!-- FAQ Item 2 -->
                    <div class="accordion-item">
                        <h2 class="accordion-header" id="headingTwo">
                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseTwo" aria-expanded="false" aria-controls="collapseTwo">
                                How long does it typically take to complete a project?
                            </button>
                        </h2>
                        <div id="collapseTwo" class="accordion-collapse collapse" aria-labelledby="headingTwo" data-bs-parent="#faqAccordion">
                            <div class="accordion-body">
                                Project timelines vary depending on the scope and complexity. A simple website might take 4-6 weeks, while a complex web application could take 3-6 months. Mobile applications typically take 2-5 months, and enterprise solutions can take 6+ months. During our initial consultation, we'll provide a more accurate timeline based on your specific requirements.
                            </div>
                        </div>
                    </div>

                    <!-- FAQ Item 3 -->
                    <div class="accordion-item">
                        <h2 class="accordion-header" id="headingThree">
                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseThree" aria-expanded="false" aria-controls="collapseThree">
                                What information do you need to provide a quote?
                            </button>
                        </h2>
                        <div id="collapseThree" class="accordion-collapse collapse" aria-labelledby="headingThree" data-bs-parent="#faqAccordion">
                            <div class="accordion-body">
                                To provide an accurate quote, we need to understand your project goals, target audience, desired features and functionality, technical requirements, design preferences, and timeline expectations. The more detailed information you can provide, the more accurate our estimate will be. Our service inquiry form is designed to gather this essential information.
                            </div>
                        </div>
                    </div>

                    <!-- FAQ Item 4 -->
                    <div class="accordion-item">
                        <h2 class="accordion-header" id="headingFour">
                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseFour" aria-expanded="false" aria-controls="collapseFour">
                                Do you provide ongoing support after project completion?
                            </button>
                        </h2>
                        <div id="collapseFour" class="accordion-collapse collapse" aria-labelledby="headingFour" data-bs-parent="#faqAccordion">
                            <div class="accordion-body">
                                Yes, we offer various support and maintenance packages to ensure your solution continues to run smoothly after launch. These packages include regular updates, security patches, performance monitoring, and technical support. We can customize a support plan based on your specific needs and budget.
                            </div>
                        </div>
                    </div>

                    <!-- FAQ Item 5 -->
                    <div class="accordion-item">
                        <h2 class="accordion-header" id="headingFive">
                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseFive" aria-expanded="false" aria-controls="collapseFive">
                                What technologies do you specialize in?
                            </button>
                        </h2>
                        <div id="collapseFive" class="accordion-collapse collapse" aria-labelledby="headingFive" data-bs-parent="#faqAccordion">
                            <div class="accordion-body">
                                We specialize in a wide range of technologies including React, Angular, Vue.js, Node.js, .NET, Python, PHP, iOS/Android native development, React Native, Flutter, AWS, Azure, Google Cloud, Docker, Kubernetes, and various database technologies. Our team stays up-to-date with the latest advancements to ensure we're using the most appropriate and efficient technologies for your project.
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Map Section -->
<section class="map-section">
    <div class="container-fluid p-0">
        <div class="map-container">
            <iframe width="600"
                    height="450"
                    style="border:0"
                    loading="lazy"
                    allowfullscreen
                    referrerpolicy="no-referrer-when-downgrade"
                    src="https://www.google.com/maps?q=33.641258,73.072129&output=embed">
            </iframe>
        </div>
    </div>
</section>

<!-- CTA Section -->
<section class="cta-section stats-section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8 text-center">
                <h2 class="mb-4">Ready to Start Your Project?</h2>
                <p class="mb-4">Let's discuss how Vexora Labs can help you achieve your business goals.</p>
                <a href="#" class="btn btn-light btn-lg">Schedule a Free Consultation</a>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <script>
        
        document.addEventListener('DOMContentLoaded', function() {
            // Form validation
            const form = document.getElementById('serviceInquiryForm');
            const successMessage = document.getElementById('formSuccessMessage');
            const submitButton = document.getElementById('submitButton');
            const resetButton = document.getElementById('resetFormButton');

            if (form) {
                form.addEventListener('submit', function(event) {
                    event.preventDefault();

                    if (!form.checkValidity()) {
                        event.stopPropagation();
                        form.classList.add('was-validated');
                        return;
                    }

                    // Show loading state
                    submitButton.disabled = true;
                    submitButton.innerHTML = '<span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span> Submitting...';

                    // Simulate form submission (replace with actual AJAX call in production)
                    setTimeout(function() {
                        form.style.display = 'none';
                        successMessage.style.display = 'block';

                        // Reset form state
                        form.reset();
                        form.classList.remove('was-validated');
                        submitButton.disabled = false;
                        submitButton.innerHTML = '<span class="btn-text">Submit Inquiry</span><span class="btn-icon"><i class="fas fa-paper-plane"></i></span>';

                        // Scroll to success message
                        successMessage.scrollIntoView({ behavior: 'smooth', block: 'start' });
                    }, 1500);
                });
            }

            // Reset form button
            if (resetButton) {
                resetButton.addEventListener('click', function() {
                    successMessage.style.display = 'none';
                    form.style.display = 'block';
                });
            }

            // Service type "Other" conditional field
            const serviceTypeSelect = document.getElementById('serviceType');

            if (serviceTypeSelect) {
                serviceTypeSelect.addEventListener('change', function() {
                    if (this.value === 'other') {
                        // Create and insert other service field if it doesn't exist
                        if (!document.getElementById('otherServiceType')) {
                            const otherServiceField = document.createElement('div');
                            otherServiceField.className = 'col-12 mt-3';
                            otherServiceField.id = 'otherServiceField';
                            otherServiceField.innerHTML = `
                                <div class="form-floating">
                                    <input type="text" class="form-control" id="otherServiceType" placeholder="Please specify" required>
                                    <label for="otherServiceType">Please specify service type</label>
                                    <div class="invalid-feedback">Please specify the service type you're interested in.</div>
                                </div>
                            `;

                            // Insert after the service type field
                            serviceTypeSelect.closest('.col-md-6').insertAdjacentElement('afterend', otherServiceField);
                        }
                    } else {
                        // Remove other service field if it exists
                        const otherServiceField = document.getElementById('otherServiceField');
                        if (otherServiceField) {
                            otherServiceField.remove();
                        }
                    }
                });
            }

            // File upload preview
            const fileInput = document.getElementById('projectFiles');
            const filePreview = document.getElementById('filePreview');

            if (fileInput && filePreview) {
                fileInput.addEventListener('change', function() {
                    filePreview.innerHTML = '';

                    if (this.files.length > 0) {
                        const fileList = document.createElement('div');
                        fileList.className = 'file-list mt-2';

                        for (let i = 0; i < this.files.length; i++) {
                            const file = this.files[i];
                            const fileItem = document.createElement('div');
                            fileItem.className = 'file-item';

                            // Determine file icon based on type
                            let fileIcon = 'fa-file';
                            if (file.type.includes('image')) fileIcon = 'fa-file-image';
                            else if (file.type.includes('pdf')) fileIcon = 'fa-file-pdf';
                            else if (file.type.includes('word')) fileIcon = 'fa-file-word';
                            else if (file.type.includes('excel')) fileIcon = 'fa-file-excel';

                            fileItem.innerHTML = `
                                <i class="fas ${fileIcon} me-2"></i>
                                <span class="file-name">${file.name}</span>
                                <span class="file-size">(${formatFileSize(file.size)})</span>
                            `;

                            fileList.appendChild(fileItem);
                        }

                        filePreview.appendChild(fileList);
                    }
                });

                // Helper function to format file size
                function formatFileSize(bytes) {
                    if (bytes === 0) return '0 Bytes';
                    const k = 1024;
                    const sizes = ['Bytes', 'KB', 'MB', 'GB'];
                    const i = Math.floor(Math.log(bytes) / Math.log(k));
                    return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + ' ' + sizes[i];
                }
            }

            // Form field animations
            const formControls = document.querySelectorAll('.form-control, .form-select');

            formControls.forEach(control => {
                // Add animation class when field is focused or has value
                function updateFieldState() {
                    if (control.value) {
                        control.classList.add('has-value');
                    } else {
                        control.classList.remove('has-value');
                    }
                }

                // Initial state
                updateFieldState();

                // Update on input
                control.addEventListener('input', updateFieldState);

                // Add focus animation
                control.addEventListener('focus', function() {
                    this.closest('.form-floating').classList.add('focused');
                });

                control.addEventListener('blur', function() {
                    this.closest('.form-floating').classList.remove('focused');
                });
            });
        });
    </script>
}
